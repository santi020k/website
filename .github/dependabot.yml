version: 2

updates:
  # pnpm catalogs are maintained deliberately because Dependabot currently
  # rewrites catalog references into lockfiles that fail frozen installs.
  # A zero version-PR limit keeps security update pull requests enabled.
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: monthly
      day: monday
      time: '06:00'
      timezone: America/Bogota
    open-pull-requests-limit: 0
    groups:
      npm-security-updates:
        applies-to: security-updates
        patterns:
          - '*'
    commit-message:
      prefix: chore(deps)

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
      day: monday
      time: '06:15'
      timezone: America/Bogota
    open-pull-requests-limit: 1
    groups:
      workflow-actions:
        patterns:
          - '*'
    commit-message:
      prefix: chore(deps)
