# Changesets

Hello and welcome! This folder is generated and managed by
[`@changesets/cli`](https://github.com/changesets/changesets).

Run `pnpm changeset` when a change should be included in a release. Commit the
generated Markdown file with the change. The release workflow collects those
files into a version pull request that updates `package.json` and
`CHANGELOG.md`.
