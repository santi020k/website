---
import { Icon } from 'astro-icon/components'

import ContactSection from '@/components/molecules/ContactSection.astro'
import GradientDivider from '@/components/atoms/GradientDivider.astro'
import NewsletterSignup from '@/components/molecules/NewsletterSignup.astro'

import { version } from '../../../package.json'

import AnimatedLogo from '@/components/atoms/AnimatedLogo.astro'
import { menuLinks, siteConfig } from '@/site.config'

const year = new Date().getFullYear()

const navLinks = [
  ...menuLinks,
  { path: '/blog/series/', title: 'Series' },
  { path: '/blog/tags/', title: 'Topics' },
  { path: '/technologies/', title: 'Technologies' },
  { path: '/uses/', title: 'Uses' }
]

const resourceLinks = [
  { icon: 'tabler:calendar-month', path: '/blog/content-calendar/', title: 'Content Calendar' },
  { icon: 'tabler:rss', path: '/feed.xml', title: 'RSS Feed' },
  { icon: 'tabler:sitemap', path: '/sitemap-index.xml', title: 'Sitemap' }
]

const currentPath = Astro.url.pathname
const isAvailable = siteConfig.availability.toLowerCase().startsWith('open')

const isLinkActive = (linkPath: string): boolean => {
  if (linkPath === '/') return currentPath === '/'

  const allLinks = [...navLinks, ...resourceLinks]

  const bestMatch = allLinks
    .filter(l => l.path !== '/' && currentPath.startsWith(l.path))
    .sort((a, b) => b.path.length - a.path.length)[0]

  return bestMatch?.path === linkPath
}
---

<footer class="relative mt-10 overflow-hidden px-4 pb-10 sm:px-6 lg:px-8">
  <GradientDivider class="pt-10" />

  <!-- Top ambient glow with floating animation -->
  <div
    aria-hidden="true"
    class="pointer-events-none absolute top-0 left-1/2 h-96 w-[80%] max-w-3xl -translate-x-1/2 rounded-full opacity-25 blur-3xl motion-safe:animate-float-y-slow motion-reduce:animate-none"
    style="background: radial-gradient(ellipse at center, hsl(var(--brand) / 0.08), transparent 70%);"
  />

  <!-- Subtle radial glow behind footer content with pulse -->
  <div
    aria-hidden="true"
    class="pointer-events-none absolute top-[10%] left-1/2 h-80 w-[70%] max-w-3xl -translate-x-1/2 rounded-full opacity-20 blur-3xl motion-safe:animate-pulse-slow motion-reduce:animate-none"
    style="background: radial-gradient(ellipse at center, hsl(var(--accent) / 0.1), transparent 70%);"
  />

  <!-- Subtle grid texture in background -->
  <div
    aria-hidden="true"
    class="pointer-events-none absolute inset-0 grid-fade opacity-40"
  />

  <div class="relative mx-auto w-full max-w-6xl">
    <!-- Morphing organic blob behind content -->
    <div
      aria-hidden="true"
      class="pointer-events-none absolute top-1/2 left-1/2 z-0 h-96 w-96 -translate-x-1/2 -translate-y-1/2 opacity-25 blur-3xl motion-safe:animate-morph motion-reduce:animate-none sm:h-[28rem] sm:w-[28rem]"
      style="background: linear-gradient(135deg, hsl(var(--brand) / 0.12), hsl(var(--accent) / 0.08), hsl(var(--glow) / 0.05));"
    />

    <!-- Unified Contact & Newsletter Section -->
    <section class="relative z-10 grid gap-8 pt-10 lg:grid-cols-[minmax(0,0.72fr)_minmax(0,1.28fr)] lg:gap-10" data-animate>
      <div class="space-y-3" data-animate>
        <p class="section-label text-brand">Get in touch</p>
        <h2 class="text-2xl font-semibold tracking-tight sm:text-3xl">
          Let's build something{' '}
          <span
            class="bg-linear-to-r from-brand via-accent to-brand bg-[length:200%_100%] bg-clip-text text-transparent motion-safe:animate-shimmer"
            style="background-size: 200% 100%;"
          >
            useful
          </span>{' '}
          together.
        </h2>
        <p class="max-w-xl text-sm text-ink-soft">
          Have a project in mind or want to compare notes on engineering? Pick a channel below and I'll get back to you quickly.
        </p>
      </div>

      <div class="relative z-10 grid gap-6" data-stagger="60">
        <ContactSection compact />
        {siteConfig.newsletter?.formAction && (
          <NewsletterSignup formAction={siteConfig.newsletter.formAction} label="Newsletter signup (footer)" variant="compact" />
        )}
      </div>
    </section>

    <GradientDivider class="pt-10" />

    <!-- Footer body grid -->
    <section
      class="
        grid gap-8 pt-10
        sm:grid-cols-2
        lg:grid-cols-[minmax(0,1.5fr)_minmax(0,0.85fr)_minmax(0,0.85fr)_minmax(0,0.75fr)]
        lg:gap-10
      "
      data-stagger="60"
    >

      <!-- Brand column -->
      <div class="space-y-6">
        <a
          aria-label="Santiago Molina — home"
          class="
            inline-block transition duration-200
            hover:opacity-80
            motion-reduce:transition-none
          "
          href="/"
        >
          <div class="h-8 w-auto dark:hidden">
            <AnimatedLogo variant="light" class="h-full w-auto" />
          </div>
          <div class="hidden h-8 w-auto dark:block">
            <AnimatedLogo variant="dark" class="h-full w-auto" />
          </div>
        </a>

        <p class="max-w-[24ch] text-sm/6 text-ink-soft">
          Engineering leader and full-stack architect. Building resilient systems and empowering developer teams.
        </p>

        <div class="space-y-2.5 text-xs text-ink-muted">
          <div class="flex items-center gap-2">
            <Icon aria-hidden="true" class="size-3.5 shrink-0" name="tabler:map-pin" />
            <span>{siteConfig.location}</span>
          </div>
          <div class="flex items-center gap-2">
            {isAvailable ?
              (
                <>
                  <span class="relative flex size-1.5 shrink-0">
                    <span class="
                      absolute inline-flex size-full animate-ping rounded-full
                      bg-success/75 opacity-75
                      motion-reduce:animate-none
                    "
                    />
                    <span class="
                      relative inline-flex size-1.5 rounded-full bg-success
                    "
                    />
                  </span>
                  <span class="text-success">{siteConfig.availability}</span>
                </>
              ) :
              (
                <span>{siteConfig.availability}</span>
              )}
          </div>
        </div>
      </div>

      <!-- Navigate column -->
      <nav aria-label="Footer navigation">
        <p class="mb-5 section-label">Navigate</p>
        <ul class="space-y-2.5">
          {navLinks.map(link => {
            const active = isLinkActive(link.path)

            return (
              <li>
                <a
                  aria-current={active ? 'page' : undefined}
                  class:list={[
                    `
                      group inline-flex items-center gap-2 text-sm
                      transition-colors duration-200
                      motion-reduce:transition-none
                    `,
                    active ?
                      'font-semibold text-brand' :
                      `
                        text-ink-soft
                        hover:text-ink
                      `
                  ]}
                  href={link.path}
                >
                  {active && (
                    <span
                      aria-hidden="true"
                      class="size-1.5 shrink-0 rounded-full bg-brand"
                    />
                  )}
                  {link.title}
                </a>
              </li>
            )
          })}
        </ul>
      </nav>

      <!-- Connect column -->
      <div>
        <p class="mb-5 section-label">Connect</p>
        <ul class="space-y-2.5">
          {siteConfig.socialLinks.map(({ href, icon, indieAuthRelMe, label }) => (
            <li>
              <a
                class="
                  group inline-flex items-center gap-2 text-sm text-ink-soft
                  transition-colors duration-200
                  hover:text-ink
                  motion-reduce:transition-none
                "
                href={href}
                rel={`noreferrer noopener ${indieAuthRelMe === false ? '' : 'me'}`}
                target="_blank"
              >
                <Icon
                  aria-hidden="true"
                  class="
                    size-4 shrink-0 text-ink-muted transition-[color,transform]
                    duration-200
                    group-hover:-translate-y-px group-hover:text-brand
                    motion-reduce:transition-none
                    motion-reduce:group-hover:translate-y-0
                  "
                  name={icon}
                />
                {label}
              </a>
            </li>
          ))}
          <li class="pt-1">
            <a
              class="
                group inline-flex items-center gap-2 text-sm text-ink-soft
                transition-colors duration-200
                hover:text-ink
                motion-reduce:transition-none
              "
              href={siteConfig.contact.resume}
              rel="noreferrer noopener"
              target="_blank"
            >
              <Icon
                aria-hidden="true"
                class="
                  size-4 shrink-0 text-ink-muted transition-[color,transform]
                  duration-200
                  group-hover:-translate-y-px group-hover:text-brand
                  motion-reduce:transition-none
                  motion-reduce:group-hover:translate-y-0
                "
                name="tabler:file-text"
              />
              Resume
            </a>
          </li>
        </ul>
      </div>

      <!-- Resources column -->
      <div>
        <p class="mb-5 section-label">Resources</p>
        <ul class="space-y-2.5">
          {resourceLinks.map(({ icon, path, title }) => {
            const active = isLinkActive(path)

            return (
              <li>
                <a
                  aria-current={active ? 'page' : undefined}
                  class:list={[
                    `
                      group inline-flex items-center gap-2 text-sm
                      transition-colors duration-200
                      motion-reduce:transition-none
                    `,
                    active ?
                      'font-semibold text-brand' :
                      `
                        text-ink-soft
                        hover:text-ink
                      `
                  ]}
                  href={path}
                >
                  <Icon
                    aria-hidden="true"
                    class:list={[
                      `
                        size-4 shrink-0 transition-[color,transform]
                        duration-200
                        group-hover:-translate-y-px
                        motion-reduce:transition-none
                        motion-reduce:group-hover:translate-y-0
                      `,
                      active ?
                        'text-brand' :
                        `
                          text-ink-muted
                          group-hover:text-brand
                        `
                    ]}
                    name={icon}
                  />
                  {title}
                </a>
              </li>
            )
          })}
        </ul>
      </div>

    </section>

    <GradientDivider class="pt-10" />

    <!-- Bottom bar -->
    <div
      class="
        flex flex-col gap-3 border-t border-line/30 pt-6 text-xs text-ink-muted
        opacity-100
        sm:flex-row sm:items-center sm:justify-between
      "
    >
      <p>&copy; {year} {siteConfig.author}.</p>

      <div class="flex flex-wrap items-center gap-x-4 gap-y-2">
        <a
          class="
            inline-flex items-center gap-1.5 text-ink-muted transition-colors
            hover:text-brand
            motion-reduce:transition-none
          "
          href="/accessibility/"
        >
          <Icon aria-hidden="true" class="size-3 shrink-0" name="tabler:accessible" />
          Accessibility
        </a>

        <a
          class="
            inline-flex items-center gap-1.5 text-ink-muted transition-colors
            hover:text-brand
            motion-reduce:transition-none
          "
          href="/privacy/"
        >
          <Icon aria-hidden="true" class="size-3 shrink-0" name="tabler:shield-lock" />
          Privacy &amp; analytics
        </a>

        <a
          class="
            inline-flex items-center gap-1 rounded-full border border-line/60
            bg-surface-muted/60 px-2 py-0.5 font-medium text-ink-muted
            transition-colors
            hover:border-brand/40 hover:text-brand
            motion-reduce:transition-none
          "
          href="https://github.com/santi020k/private-website"
          rel="noreferrer noopener"
          target="_blank"
        >
          <Icon aria-hidden="true" class="size-3" name="tabler:brand-github" />
          v{version}
        </a>
      </div>
    </div>

  </div>
</footer>
