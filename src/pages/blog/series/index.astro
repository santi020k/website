---
import ButtonLink from '@/components/atoms/ButtonLink.astro'
import PageHero from '@/components/molecules/PageHero.astro'
import SectionHeader from '@/components/molecules/SectionHeader.astro'
import SeriesCard from '@/components/molecules/SeriesCard.astro'
import StatCard from '@/components/molecules/StatCard.astro'
import PageSection from '@/components/organisms/PageSection.astro'
import StatsGrid from '@/components/organisms/StatsGrid.astro'
import PageLayout from '@/layouts/Base.astro'

import { editorialCalendar } from '@/data/editorial-calendar'
import { getSeriesSummaries } from '@/data/series'

const seriesSummaries = await getSeriesSummaries()
const activeSeries = seriesSummaries.filter(item => item.postCount > 0 && item.series.data.status === 'active')
const postsGroupedIntoSeries = activeSeries.reduce((count, item) => count + item.postCount, 0)
const roadmapSeriesCount = new Set(editorialCalendar.map(entry => entry.seriesId).filter(Boolean)).size

const breadcrumbs = [
  { name: 'Home', url: '/' },
  { name: 'Blog', url: '/blog/' },
  { name: 'Series', url: '/blog/series/' }
]
---

<PageLayout
  meta={{
    breadcrumbs,
    description: 'Grouped reading tracks for Santiago Molina\'s writing on architecture, tooling, testing, and delivery systems.',
    title: 'Blog Series — Santiago Molina'
  }}
>
  <PageHero
    description="Grouped reading paths — follow a topic from start to finish without hunting the archive."
    eyebrow="Series"
    gradientText="in order."
    metaItems={['Reading tracks', 'Practical sequence', 'Connected ideas']}
    title="Follow related posts"
  >
    <ButtonLink href="/blog/" showArrow variant="secondary">
      Back to blog
    </ButtonLink>
    <ButtonLink href="/blog/content-calendar/" showArrow variant="secondary">
      Content calendar
    </ButtonLink>
  </PageHero>

  <StatsGrid>
    <StatCard
      description="Ongoing reading tracks with at least one published post."
      label="Active series"
      value={String(activeSeries.length)}
    />
    <StatCard
      description="Posts that belong to a series rather than standing alone in the archive."
      label="Posts grouped"
      value={String(postsGroupedIntoSeries)}
    />
    <StatCard
      description="Distinct series planned or in progress on the editorial calendar."
      label="Roadmap tracks"
      value={String(roadmapSeriesCount)}
    />
  </StatsGrid>

  <PageSection>
    <SectionHeader
      description="Each series is a self-contained reading track — start at post one, follow the thread, finish with a complete mental model."
      eyebrow="Series"
      title="Current series on the site."
    />

    <div
      class="
        grid gap-6
        lg:grid-cols-2
      "
      data-stagger="70"
    >
      {activeSeries.map(item => (
        <SeriesCard postCount={item.postCount} series={item.series} />
      ))}
    </div>
  </PageSection>
</PageLayout>
